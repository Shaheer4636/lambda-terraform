$ErrorActionPreference='Stop'; Compress-Archive -Path "$PWD\lambda_generate_uptime.py" -DestinationPath "$PWD\lambda.zip" -Force; terraform init -upgrade; terraform apply -auto-approve; aws lambda invoke --function-name uptime-uptime-lambda --region us-east-1 --cli-binary-format raw-in-base64-out --payload '{}' out.json; $yy=(Get-Date).ToUniversalTime().AddMonths(-1).ToString('yyyy'); $mm=(Get-Date).ToUniversalTime().AddMonths(-1).ToString('MM'); aws s3 ls "s3://lambda-output-report-000000987123/reports/cdp/$yy/$mm/" --region us-east-1
